import { Given, When, Then } from '@badeball/cypress-cucumber-preprocessor';

Given('eu envio um GET para a API', () => {
   
  });
  
  When("o status code deve ser 200", () => {
    
  });
  
  Then("o campo {string} da estrutura {string} deve ser exibido", () => {
   
  });